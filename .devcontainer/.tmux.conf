# Set keyboard navigation to `vi` mode (defaut: `emacs` mode).
set-window-option -g mode-keys vi

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Set history buffer to 10 000 (default is 2 000).
set -g history-limit 10000

# Use mouse
set -g mouse off
