# == Schema Information
#
# Table name: splits
#
#  id                 :uuid             not null, primary key
#  created_at         :datetime         not null
#  updated_at         :datetime         not null
#  exchange_id        :uuid             not null, indexed, indexed => [position]
#  register_id        :uuid             not null, indexed
#  position           :integer          not null, indexed => [exchange_id]
#  amount             :integer          not null
#  counterpart_amount :integer
#  memo               :text
#  status             :enum             default("uncleared"), not null
#
joe_fruits_1_joe_fruits:
  exchange: joe_fruits_1
  register: joe_fruits
  position: 0
  amount: 10
  counterpart_amount: -10
