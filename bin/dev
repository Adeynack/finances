#!/usr/bin/env bash
set -e

(which overmind > /dev/null) || bundle install # `overmind` is part of the `Gemfile`.

echo "---[ Starting development environment ]---"
overmind status > /dev/null 2>&1 || overmind quit || rm -rf .overmind.sock
overmind s -f Procfile.dev -p ${PORT:-30001} -P 10
